# RESTful-Service-for-Employee-Management
Objective:
Build a simple REST API using Spring Boot, Spring Web, and Spring Data JPA to perform basic CRUD (Create, Read, Update, Delete) operations on an Employee entity.
